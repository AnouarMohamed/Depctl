# Generated by depctl
# This is an example environment file. Copy to .env and fill in the values.

{{range .Env.Required}}
{{.}}={{if eq . "NODE_ENV"}}production{{else if eq . "APP_ENV"}}production{{else if eq . "DEBUG"}}false{{end}}
{{- end}}
{{- if .Env.Sensitive}}

# Sensitive variables
{{- range .Env.Sensitive}}
# {{.}}=[HIDDEN]
{{- end}}
{{- end}}
